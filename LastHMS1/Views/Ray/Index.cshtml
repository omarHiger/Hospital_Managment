@model IEnumerable<LastHMS1.Models.Ray>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Ray_Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ray_Type)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Ray_Result)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ray_Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ray_Type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Ray_Result)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Ray_Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Ray_Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Ray_Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
